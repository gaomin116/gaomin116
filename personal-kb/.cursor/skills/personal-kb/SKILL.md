---
description: 检索并引用 personal-kb 本地 Markdown 知识库
---

# 个人知识库检索

当需要查找用户自己的笔记、既往结论、术语或项目备忘时，使用 MCP `personal-kb`：

1. `kb_search` — 用关键词检索（中文短语也可）
2. `kb_get` — 按相对路径读取全文
3. `kb_list` — 浏览目录前缀
4. `kb_stats` / `kb_reindex` — 诊断与重建索引

回答时附上笔记路径；未命中时明确说明，并提示把 Markdown 放入 `personal-kb/kb/`。
