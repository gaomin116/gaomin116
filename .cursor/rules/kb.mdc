---
description: 个人知识库检索约定 — 优先用 personal-kb MCP
alwaysApply: true
---

# 个人知识库（personal-kb）

当用户询问个人笔记、资料、既往结论、或「知识库里有没有…」时：

1. **先调用 MCP `personal-kb`**：`kb_search` → 需要全文时再 `kb_get`
2. 回答时**引用笔记路径**（如 `topics/cursor-kb.md`），不要编造未检索到的内容
3. 若检索为空：说明未命中，可建议用户把笔记放进 `personal-kb/kb/` 后执行 `python3 personal-kb/mcp/kb.py index`
4. 批量改笔记后可用 `kb_reindex`

目录：`personal-kb/kb/{inbox,topics,projects,refs}/`
