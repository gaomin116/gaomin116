## Cursor Cloud specific instructions

- This repository is currently a GitHub profile README repository. It has no application service, package manager manifest, lockfile, test suite, lint script, build script, Docker config, or devcontainer config.
- No local services are required to develop or validate the current repository contents. If future commits add an application or package manifest, follow those new project files instead of these README-only assumptions.
