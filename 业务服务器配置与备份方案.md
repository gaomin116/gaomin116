# 业务服务器清单、架构说明与备份方案

> 慢性病管理系统 / 微信 H5 业务

## 一、业务服务器清单及所需服务器资源

本系统采用「内网应用服务器 + DMZ 区前置机」两台虚拟机部署。应用服务器承载主程序与数据库；前置机承载 Nginx 及对外接口程序，用于开放外网访问手机端应用。

### 表1 硬件环境配置表

| 序号 | 硬件设备名称 | 配置要求 | 数量 | 备注 |
| --- | --- | --- | --- | --- |
| 1 | 应用服务器（内网 / ManBing-app） | （1）4 颗处理器，2.10GHz（Intel Xeon Gold 5318Y）；（2）内存 16GB；（3）2 块 VMware 虚拟 SCSI 硬盘；（4）1 块 vmxnet3 虚拟万兆网卡；（5）Windows Server 2019 Standard（64 位） | 1 台 | 医院内网 VMware 虚机；部署 Tomcat + MariaDB；全名 ManBing-app.hnzyfy.com；域 hnzyfy.com |
| 2 | 前置机（DMZ 区） | （1）4 颗处理器，2.10GHz（Intel Xeon Gold 5318Y）；（2）内存 8GB；（3）存储 500GB；（4）网络位于 DMZ 区（VMware 虚拟网卡）；（5）Windows Server 2019 Standard（64 位） | 1 台 | DMZ 区 VMware 虚机；部署 Nginx、微信接口、短信接口；对外提供手机端 H5 入口 |

### 软件组件部署一览

| 服务器 | 部署组件 | 主要用途 | 参考地址 |
| --- | --- | --- | --- |
| 应用服务器（内网） | Tomcat、MariaDB | 主程序运行与业务数据存储 | 192.168.8.152 |
| 前置机（DMZ） | Nginx、微信接口、短信接口 | 外网访问入口及第三方接口转发 | 172.17.4.87 |

## 二、业务服务器架构说明

系统采用分层部署：外网用户通过医院公网 IP 及二级域名访问 DMZ 区前置机；前置机经 Nginx 反向代理及接口程序，单向访问内网应用服务器上的应用服务与数据库服务。内网服务器不直接对公网开放。

### 2.1 架构拓扑（逻辑）

手机端用户 / 微信 H5 → 医院二级域名解析至医院公网 IP → 公网 IP 代理转发至 DMZ 前置机（Nginx，端口 30348）→ 前置机单向访问内网应用服务器（端口 3307 数据库、18989 Nginx/应用）→ 前置机按需访问腾讯微信接口域名（`api.weixin.qq.com`、`mp.weixin.qq.com`）。

### 2.2 网络开通要求摘要

| 序号 | 源 | 目标 | 协议/端口 | 用途 |
| --- | --- | --- | --- | --- |
| 1 | 前置机 172.17.4.87 | api.weixin.qq.com / mp.weixin.qq.com | HTTPS / TCP | 访问腾讯微信原生接口（单向） |
| 2 | 前置机 172.17.4.87 | 应用服务器 192.168.8.152 | TCP 3307、18989 | 访问数据库及 Nginx/应用端口（单向） |
| 3 | 医院公网 IP | 前置机 172.17.4.87 | TCP 30348 | 公网代理访问前置机 Nginx 服务（单向） |
| 4 | 医院二级域名 | 医院公网 IP | DNS 解析 | 微信 H5 页面域名解析 |

说明：医院公网 IP 及二级域名由医院侧提供；短信接口出网地址如有额外白名单要求，部署时另行确认开通。

## 三、两机房部署说明

经项目沟通确认，**本期不实施两机房（双中心）部署**。系统按单机房方案建设：内网应用服务器 1 台 + DMZ 前置机 1 台。如后续医院有同城灾备或两机房需求，可另行评估主备切换、数据同步及网络专线条件后补充方案。

- 本期结论：不支持/不实施两机房双活或双中心部署。
- 本期部署模式：单机房，虚拟化部署（VMware）。
- 可用性保障：依赖本机房虚拟化平台高可用能力及下文备份恢复方案。

## 四、备份方案

### 4.1 备份对象与策略

| 项目 | 说明 |
| --- | --- |
| 备份对象 | MariaDB 业务数据库（全量） |
| 备份方式 | mysqldump 逻辑全量备份 |
| 备份频率 | 全量备份（建议每日执行，具体时间窗口与医院运维协商） |
| 保留周期 | 保留半年（约 180 天） |
| 存放位置 | 应用服务器本机磁盘 |
| 恢复责任 | 由乙方运维人员负责执行恢复 |
| 恢复说明 | 根据备份文件使用 mysql 客户端导入恢复；恢复前确认停服窗口 |

### 4.2 备份与恢复流程要点

- 日常：在应用服务器上按计划执行 mysqldump 全量导出，备份文件保留于本机指定目录，超期文件按半年策略清理。
- 异常恢复：由乙方运维定位故障点，选用对应时点备份文件完成数据库恢复，并验证业务可用性。
- 说明：本方案为数据库级逻辑备份；应用配置及前置机 Nginx/接口配置变更后，建议同步留存配置副本。虚拟机级快照可由医院虚拟化平台按院内规范另行管理。

### 4.3 建议关注事项（供医院评审）

- 备份存于本机，存在与生产机同损风险；如医院有统一备份平台或异地存储，建议将 mysqldump 结果同步拷贝一份至备份平台。
- 应用服务器表中「2 块虚拟 SCSI 硬盘」未标注容量，建议部署前与医院确认数据盘容量，确保可容纳半年备份文件增长。

## 五、配置结论汇总

- 服务器数量：2 台虚拟机（内网应用服务器 1 台、DMZ 前置机 1 台）。
- 核心资源：应用服务器 4vCPU / 16GB；前置机 4vCPU / 8GB / 500GB；操作系统均为 Windows Server 2019 Standard。
- 架构：前置机对外、应用服务器对内；数据库与主程序同机部署于内网。
- 两机房：本期不实施。
- 备份：mysqldump 全量备份数据库，保留半年，存本机，由乙方运维恢复。
